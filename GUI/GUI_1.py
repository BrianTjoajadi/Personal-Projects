from tkinter import *

window = Tk() #create a window
theLabel = Label(window, text = "Let's study some python GUI") #create text label
theLabel.pack() #packs it in window

window.mainloop() #for GUI application to remain open